<Window x:Class="WinLicApp.FullLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="650" Width="900" MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner" Background="#f8f7fe" FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="SideBtn" TargetType="Button">
            <Setter Property="Background" Value="#e8e4f5"/>
            <Setter Property="Foreground" Value="#2d2359"/>
            <Setter Property="BorderBrush" Value="#c9c2e8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#ddd7f0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#cfc9e8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="UtilBtn" TargetType="Button" BasedOn="{StaticResource SideBtn}">
            <Setter Property="Background" Value="#f4f2f9"/>
            <Setter Property="BorderBrush" Value="#d4cdf0"/>
            <Setter Property="Foreground" Value="#9c98b5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#ece9fb" Padding="15,10">
            <DockPanel>
                <TextBlock x:Name="TxtMode" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#555" FontWeight="SemiBold" FontSize="13"/>
                <TextBlock x:Name="TxtTitle" VerticalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="#333"/>
            </DockPanel>
        </Border>

        <!-- Log Area -->
        <Border Grid.Row="1" Margin="10" CornerRadius="6" Background="#1e1b2e" ClipToBounds="True">
            <TextBox x:Name="TxtLog" FontFamily="Consolas" FontSize="12" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="Transparent" Foreground="#e2e8f0"
                     BorderThickness="0" Padding="10"/>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#ece9fb" BorderBrush="#d4cdf0" BorderThickness="0,1,0,0" Padding="15,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="TxtStatus" VerticalAlignment="Center" Foreground="Green" Margin="0,0,15,0" Visibility="Hidden" FontWeight="SemiBold"/>
                
                <Button x:Name="BtnSave" Click="BtnSave_Click" Margin="0,0,10,0" Style="{StaticResource SideBtn}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" Margin="0,0,5,0"/>
                        <TextBlock x:Name="TxtBtnSave" Text="Save"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnCopy" Click="BtnCopy_Click" Margin="0,0,10,0" Style="{StaticResource SideBtn}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" Margin="0,0,5,0"/>
                        <TextBlock x:Name="TxtBtnCopy" Text="Copy"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnClose" Click="BtnClose_Click" Style="{StaticResource UtilBtn}">
                    <TextBlock x:Name="TxtBtnClose" Text="Close"/>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
